# PostgreSQL connection strings — reemplaza los valores de ejemplo
# Formato general:
#   dialect+driver://user:password@host:port/database
# Ejemplos (descomenta la que uses y rellena credenciales reales):

# --- Recomendado (async) para SQLAlchemy async + asyncpg
DATABASE_URL=postgresql+asyncpg://postgres:30237186vJuan!@127.0.0.1:5432/hirefocus

# --- Alternativa sync (psycopg2)
# DATABASE_URL=postgresql+psycopg2://hireuser:hirepass@localhost:5432/hiredb

# --- Si usas Docker y la app corre fuera del contenedor, usa localhost
# --- Si la app corre dentro de la misma red Docker, usa el nombre del servicio como host:
# DATABASE_URL=postgresql+asyncpg://hireuser:hirepass@postgres:5432/hiredb

# Nota: no subir este archivo a un repositorio público. Añade ".env" a tu .gitignore.

# Stream Chat Configuration
STREAM_API_KEY=4s855nsgjv84
STREAM_API_SECRET=tnh29ur2q4e5qeu5yz43wcttrvtzd3dxarskfcfr5exwbkuujpqkyfyz4xqy64dp